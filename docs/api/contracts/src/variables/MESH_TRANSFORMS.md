[**seans-mfe-tool API reference**](../../../README.md)

***

[seans-mfe-tool API reference](../../../README.md) / [contracts/src](../README.md) / MESH\_TRANSFORMS

# Variable: MESH\_TRANSFORMS

> `const` **MESH\_TRANSFORMS**: readonly \[`"namingConvention"`, `"rateLimit"`, `"filterSchema"`, `"resolversComposition"`, `"cache"`, `"prefix"`, `"rename"`, `"encapsulate"`, `"federation"`, `"extend"`, `"replace"`, `"typeMerging"`, `"hoistField"`, `"bare"`\]

Defined in: [packages/contracts/src/mesh-catalog.ts:61](https://github.com/falese/seans-mfe-tool/blob/main/packages/contracts/src/mesh-catalog.ts#L61)

Mesh transforms — `@graphql-mesh/transform-*`, declared in the manifest's
top-level `transforms` array or per-source `data.sources[].transforms`, and
rendered into `.meshrc.yaml` under `transforms:`.
