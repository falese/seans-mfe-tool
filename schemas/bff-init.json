{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://seans-mfe.dev/schemas/bff:init.json",
  "title": "bff:init",
  "description": "Initialize a new BFF project or add BFF to existing project",
  "input": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "BFF project name (optional — omit to add to existing project)"
      },
      "dry-run": {
        "type": "boolean",
        "description": "Preview changes without writing them"
      },
      "port": {
        "type": "string",
        "description": "Port number for the BFF server",
        "default": "3000"
      },
      "specs": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "OpenAPI specification file(s)"
      },
      "no-static": {
        "type": "boolean",
        "description": "Create standalone BFF without static asset serving"
      },
      "project-version": {
        "type": "string",
        "description": "Project version",
        "default": "1.0.0"
      }
    },
    "required": [],
    "x-positional": [
      "name"
    ]
  },
  "output": {
    "type": "object",
    "required": [
      "name",
      "port",
      "sources",
      "generatedFiles",
      "dryRun"
    ],
    "properties": {
      "name": {
        "type": "string"
      },
      "port": {
        "type": "number"
      },
      "sources": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "generatedFiles": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "dryRun": {
        "type": "boolean"
      },
      "plannedChanges": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [
            "op",
            "target"
          ],
          "properties": {
            "op": {
              "type": "string",
              "enum": [
                "create",
                "overwrite",
                "skip",
                "spawn"
              ]
            },
            "target": {
              "type": "string"
            },
            "detail": {
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "additionalProperties": false
  },
  "errorCodes": [
    0,
    1,
    2,
    64,
    65,
    66,
    69,
    70,
    77,
    124
  ]
}
