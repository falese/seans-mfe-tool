{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://seans-mfe.dev/schemas/remote:generate.json",
  "title": "remote:generate",
  "description": "Generate files from mfe-manifest.yaml capabilities",
  "input": {
    "type": "object",
    "properties": {
      "dry-run": {
        "type": "boolean",
        "description": "Show what would be generated without writing"
      },
      "force": {
        "type": "boolean",
        "description": "Re-seed developer-owned scaffolding (App.tsx, package.json, bundler config, …) from the current templates, REPLACING your edits. Capability feature files are never re-seeded. Preview with --dry-run first."
      },
      "swift": {
        "type": "boolean",
        "description": "Declare and generate a Swift Package (native iOS) from this same manifest. Adds a targets.swift block to mfe-manifest.yaml if absent, then scaffolds swift/ beside the web build. The web build is unchanged."
      }
    },
    "required": [],
    "x-positional": []
  },
  "output": {
    "type": "object",
    "required": [
      "generated",
      "skipped",
      "errors",
      "preserved",
      "reseeded",
      "dryRun"
    ],
    "properties": {
      "generated": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "skipped": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "errors": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "preserved": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Capability names whose feature files were preserved (already implemented)."
      },
      "reseeded": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Developer-owned files that existed and were replaced because `--force` was\npassed (ADR-091). Empty on every run without the flag."
      },
      "dryRun": {
        "type": "boolean"
      },
      "plannedChanges": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [
            "op",
            "target"
          ],
          "properties": {
            "op": {
              "type": "string",
              "enum": [
                "create",
                "overwrite",
                "reseed",
                "skip",
                "spawn"
              ],
              "description": "`overwrite` re-stamps a generator-owned file; `skip` leaves a\ndeveloper-owned one alone. `reseed` is the third outcome ADR-091 adds —\na developer-owned file that exists and WILL be replaced because `--force`\nwas passed. It is distinct from `overwrite` because it is the only op that\ncan destroy work, and a dry run that calls it `overwrite` hides that."
            },
            "target": {
              "type": "string"
            },
            "detail": {
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "additionalProperties": false
  },
  "errorCodes": [
    0,
    1,
    2,
    64,
    65,
    66,
    69,
    70,
    77,
    124
  ]
}
