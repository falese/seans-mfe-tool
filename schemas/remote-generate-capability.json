{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://seans-mfe.dev/schemas/remote:generate:capability.json",
  "title": "remote:generate:capability",
  "description": "Generate a single capability from mfe-manifest.yaml",
  "input": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Capability name to generate"
      },
      "dry-run": {
        "type": "boolean",
        "description": "Show what would be generated without writing"
      },
      "force": {
        "type": "boolean",
        "description": "Re-seed developer-owned scaffolding, replacing your edits to it (ADR-089). Does not touch a capability that is already implemented."
      }
    },
    "required": [
      "name"
    ],
    "x-positional": [
      "name"
    ]
  },
  "output": {
    "type": "object",
    "required": [
      "capabilityName",
      "generated",
      "skipped",
      "errors",
      "reseeded",
      "dryRun"
    ],
    "properties": {
      "capabilityName": {
        "type": "string"
      },
      "generated": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "skipped": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "errors": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "reseeded": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Developer-owned files that existed and were replaced because `--force` was\npassed (ADR-091). Empty on every run without the flag.\n\nPresent for the same reason it is on `RemoteGenerateResult`: this command\nforwards `--force` to the same writer, so it reaches the same files, and a\ncaller that cannot tell a re-seed from a first write cannot warn about it."
      },
      "dryRun": {
        "type": "boolean"
      },
      "plannedChanges": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [
            "op",
            "target"
          ],
          "properties": {
            "op": {
              "type": "string",
              "enum": [
                "create",
                "overwrite",
                "reseed",
                "skip",
                "spawn"
              ],
              "description": "`overwrite` re-stamps a generator-owned file; `skip` leaves a\ndeveloper-owned one alone. `reseed` is the third outcome ADR-091 adds —\na developer-owned file that exists and WILL be replaced because `--force`\nwas passed. It is distinct from `overwrite` because it is the only op that\ncan destroy work, and a dry run that calls it `overwrite` hides that."
            },
            "target": {
              "type": "string"
            },
            "detail": {
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "additionalProperties": false
  },
  "errorCodes": [
    0,
    1,
    2,
    64,
    65,
    66,
    69,
    70,
    77,
    124
  ]
}
