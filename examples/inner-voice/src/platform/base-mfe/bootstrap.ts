
/**
 * innervoiceMFE bootstrap
 *
 * Instantiates innervoiceMFE and calls load() so the full doLoad lifecycle
 * runs and its console logs are visible in the browser DevTools.
 *
 * This fires once when the Module Federation remote bundle is first imported
 * by the shell (i.e. when remote.tsx is evaluated).
 *
 * @generated By seans-mfe-tool CLI — regenerated on every codegen run.
 *            Do not edit by hand; changes will be overwritten.
 */
import { innervoiceMFE } from './mfe';

const manifest = {
  "name": "inner-voice",
  "version": "1.0.0",
  "type": "remote",
  "language": "typescript",
  "framework": "react",
  "bundler": "rspack",
  "description": "No-send-button thinking interface. Streams tokens from the local MLX inference server (coder serve) as the user thinks out loud, extracts concept threads in real time, and feeds them back as the input layer for the agentic delivery loop.\n",
  "endpoint": "http://localhost:3009",
  "remoteEntry": "http://localhost:3009/remoteEntry.js",
  "discovery": "http://localhost:3009/.well-known/mfe-manifest.yaml",
  "capabilities": [
    {
      "InnerVoice": {
        "type": "domain",
        "description": "No-send-button thinking interface. Streams tokens from the local MLX inference server as the user types. Extracts concept threads in real time. Input layer for the agentic delivery loop.\n"
      }
    },
    {
      "Load": {
        "type": "platform",
        "lifecycle": {
          "error": [
            {
              "onLoadError": {
                "handler": "onLoadError",
                "contained": true
              }
            }
          ]
        }
      }
    }
  ],
  "dependencies": {
    "runtime": {
      "react": "^18.0.0",
      "react-dom": "^18.0.0"
    }
  }
};

export const mfe = new innervoiceMFE(manifest);

export const mfeReady: Promise<void> = mfe
  .load({
    inputs: {
      remoteEntry: manifest.remoteEntry,
    },
  })
  .then((result) => {
    console.log(
      '[innervoiceMFE] bootstrap load() complete \u2014 status:',
      result.status,
      '\u2014 components:',
      result.availableComponents
    );
  })
  .catch((err) => {
    console.error('[innervoiceMFE] bootstrap load() failed:', err);
  });
