

/**
 * Framework-agnostic placeholder for a component type.
 * For Angular, replace with `Type<unknown>` from '@angular/core' once it's
 * convenient (e.g. `import { Type } from '@angular/core'`).
 */
type ComponentType = unknown;

// ---------------------------------------------------------------------------
// DockingBoard — input / output types
// ---------------------------------------------------------------------------

export interface DockingBoardInputs {

}

export interface DockingBoardOutputs {

}

// ---------------------------------------------------------------------------
// BerthTile — input / output types
// ---------------------------------------------------------------------------

export interface BerthTileInputs {

}

export interface BerthTileOutputs {

}

// ---------------------------------------------------------------------------
// TrafficLog — input / output types
// ---------------------------------------------------------------------------

export interface TrafficLogInputs {

}

export interface TrafficLogOutputs {

}

// ---------------------------------------------------------------------------
// load — input / output types
// ---------------------------------------------------------------------------

export interface loadInputs {

}

export interface loadOutputs {

}

// ---------------------------------------------------------------------------
// render — input / output types
// ---------------------------------------------------------------------------

export interface renderInputs {

}

export interface renderOutputs {

}

