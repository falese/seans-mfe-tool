{
  "//": "Yours to edit — the generator seeds this once and never overwrites it. The compiler settings the platform needs live in tsconfig.platform.json (ADR-085), which this extends.",
  "extends": "./tsconfig.platform.json",
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.ts",
    "**/*.test.tsx"
  ]
}
