// Intentionally empty. `LeaderboardEntry` is not an entity — the generator creates a table
// for every schema in the spec, including request bodies and response
// projections. Seeding it would invent rows that no endpoint reads.
const ModelSeed = [];

module.exports = ModelSeed;
