# Generated by scripts/generate-games.mjs — do not edit by hand.
# Usage: docker compose -f docker-compose.yaml -f docker-compose.games.yaml up -d
services:
  abc-kids-counting-stars:
    build: ./counting-stars
    ports:
      - "3005:3005"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3005/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-letter-pop:
    build: ./letter-pop
    ports:
      - "3006:3006"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3006/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-shape-sorter:
    build: ./shape-sorter
    ports:
      - "3007:3007"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3007/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-color-mixer:
    build: ./color-mixer
    ports:
      - "3008:3008"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3008/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-animal-sounds:
    build: ./animal-sounds
    ports:
      - "3009:3009"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3009/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-memory-match:
    build: ./memory-match
    ports:
      - "3010:3010"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3010/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-rocket-math:
    build: ./rocket-math
    ports:
      - "3011:3011"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3011/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-word-builder:
    build: ./word-builder
    ports:
      - "3012:3012"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3012/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-rhythm-tap:
    build: ./rhythm-tap
    ports:
      - "3013:3013"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3013/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12

  abc-kids-maze-runner:
    build: ./maze-runner
    ports:
      - "3014:3014"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3014/remoteEntry.js"]
      interval: 10s
      timeout: 3s
      retries: 12
