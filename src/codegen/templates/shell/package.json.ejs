{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "description": "Shell (host) MFE app for the <%= projectName %> platform — generated by seans-mfe-tool (ADR-016)",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build",
    "serve": "serve dist -p <%= port %>"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@rspack/cli": "^0.5.0",
    "@rspack/core": "^0.5.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "serve": "^14.2.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
