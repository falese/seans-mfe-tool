/**
 * Shell entry point for <%= projectName %>
 *
 * Bootstraps the MFEOrchestrator, which:
 *   1. Connects to the ShellDaemon via GraphQL-WS
 *   2. Receives Component envelopes (type = MFE name) from the daemon
 *   3. Dynamically loads each MFE via Module Federation
 *   4. Sends user actions back through the daemon to the registry
 *
 * Generated by seans-mfe-tool (ADR-016)
 */
import React from 'react';
import { createRoot } from 'react-dom/client';
import { MFEOrchestrator } from './MFEOrchestrator';

const container = document.getElementById('root');
if (!container) throw new Error('Root element #root not found');

const root = createRoot(container);

root.render(
  <React.StrictMode>
    <MFEOrchestrator
      daemonUrl={`ws://${window.location.hostname}:<%= daemonPort %>/graphql`}
    />
  </React.StrictMode>,
);
