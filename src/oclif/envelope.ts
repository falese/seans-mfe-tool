// Shim — re-exports from @seans-mfe/contracts for one release cycle.
export {
  EXIT_CODES,
  exitCodeFor,
  formatSuccess,
  formatError,
} from '@seans-mfe/contracts';
export type { CommandResult, CommandError, ExitCodeKey } from '@seans-mfe/contracts';
