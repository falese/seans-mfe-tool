export { ReactRspackPlugin } from './plugin';
import { ReactRspackPlugin } from './plugin';

/** Well-known export for loadFrameworkPlugin() resolution. */
export const frameworkPlugin = new ReactRspackPlugin();
