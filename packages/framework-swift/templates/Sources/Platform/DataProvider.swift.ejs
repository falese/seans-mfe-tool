// Generated by seans-mfe-tool. Do not edit — regenerated on every `remote:generate`.
//
// The injected data provider (ADR-096).
//
// A PROTOCOL, not a generated GraphQL client. Generating typed query structs
// needs schema introspection at codegen time and is a separate scope; the
// honest minimum is a seam the host fills.
<% if (bffEndpoint) { -%>
//
// This MFE's BFF serves GraphQL at:
//     <%= bffEndpoint %>
// An implementation will normally POST GraphQL documents there.
<% } -%>

import Foundation

public protocol <%= moduleName %>DataProvider: Sendable {
<% domainCapabilities.forEach(function (cap) { -%>
    /// Data backing the `<%= cap %>` capability.
    func <%= camel(cap) %>() async throws -> <%= cap %>Outputs

<% }); -%>
}
