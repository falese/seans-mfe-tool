{
  "//": "Yours to edit — the generator seeds this once and never overwrites it. The compiler settings the platform's own emitted code depends on live in tsconfig.platform.json (ADR-085), which this extends. `extends` is a merge, so anything you set here wins over the platform's.",
  "extends": "./tsconfig.platform.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": ".",
    "declaration": true
  },
  "include": ["*.ts", "src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["node_modules", "dist"]
}
