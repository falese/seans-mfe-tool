// Generated by seans-mfe-tool. Do not edit — regenerated on every `remote:generate`.
//
// Declares the developer-owned capability modules beside this file.
//
// Generator-owned so a capability added to the manifest is declared the moment
// regeneration seeds its file. The files themselves are yours.
<% if (hasBff) { -%>
//
// Each `<capability>_query` module holds the GraphQL document the generated
// `platform::bff_data_provider` sends for that capability (ADR-096 §7).
<% if (domainCapabilities.length > 0) { -%>

<% } -%>
<% domainCapabilities.map(function (cap) { return snake(cap); }).sort().forEach(function (mod) { -%>
pub mod <%- mod %>_query;
<% }); -%>
<% } else { -%>
//
// This MFE declares no `data:` section, so there is no BFF and no query
// document to seed. Put capability code in modules of your own under
// `src/` and declare them in `lib.rs`.
<% } -%>
